spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/cangjie_agent}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
logging:
  level:
    root: info
    cn.cangjiecloud: info
