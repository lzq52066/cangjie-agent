#!/usr/bin/env bash
# ============================================================
# 90 - 停止服务（保留数据卷，数据不丢）
# ============================================================
set -euo pipefail
cd "$(dirname "$0")"

docker compose stop
echo "[90] 已停止（数据卷保留，再次执行 ./04-start.sh 即可恢复）"
