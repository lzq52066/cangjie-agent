#!/usr/bin/env bash
# ============================================================
# 90 - 停止服务（容器停止，数据保留在宿主机 ./data）
# ============================================================
set -euo pipefail
cd "$(dirname "$0")"

docker compose stop
echo "[90] 已停止（数据保留在 ./data，再次执行 ./04-start.sh 即可恢复）"
