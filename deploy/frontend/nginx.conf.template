# 纯 HTTP 配置（未放置证书时由 10-nginx-conf.sh 选用）

# MinIO 控制台需要 WebSocket（实时日志等）
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

server {
    listen 80;
    server_name _;

    include /etc/nginx/locations.conf;
}
